<%= form_with(model: guid) do |form| %>
  <% if guid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(guid.errors.count, "error") %> prohibited this guid from being saved:</h2>

      <ul>
        <% guid.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
